# Production: public Nest origin (Render/Railway). Never use localhost on Vercel.
# API_REWRITE_TARGET=https://se7en-api.onrender.com

# Local Next.js rewrites /api to this origin.
API_REWRITE_TARGET=http://127.0.0.1:3000
